version: '3.8'

services:
  web:
    build: ./app
    container_name: todo-web
    expose:
      - "5000"

  nginx:
    image: nginx:latest
    container_name: todo-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web
